[package]
name = "arnes-desktop"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[build-dependencies]
tauri-build = { version = "2.6", features = [] }

[dependencies]
arnes-host-broker = { path = "../crates/host-broker" }
tauri = { version = "2.11", features = [] }
